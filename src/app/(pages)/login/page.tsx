import { LoginForm } from "@/app/components/LoginForm/LoginForm";

export default function LoginPage() {
  return (
    <main className="min-h-dvh px-4 py-10 flex items-center justify-center">
      <LoginForm />
    </main>
  );
}
